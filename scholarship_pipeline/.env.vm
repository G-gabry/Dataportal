# .env.vm — Environment for running the pipeline NATIVELY on the VM
# Copy this to ~/scholarship_pipeline/.env on the VM

# ── Database (Cloud SQL — same from VM) ──────────────────────────────────────
POSTGRES_USER=dataportal
POSTGRES_PASSWORD=DataPortal2024
POSTGRES_DB=data_portal
POSTGRES_HOST=34.18.45.84
POSTGRES_PORT=5432
DATABASE_URL=postgresql://dataportal:DataPortal2024@34.18.45.84:5432/data_portal

# ── Backend / Portal API ──────────────────────────────────────────────────────
# Backend is deployed on Cloud Run — pipeline fetches sources from and saves items to it
PORTAL_API_BASE=https://data-portal-backend-48832924533.me-central1.run.app

# ── AI Providers ──────────────────────────────────────────────────────────────
GEMINI_API_KEY=AIzaSyAGO994FBa2zpKIOov_laUIHMKJ0cs4Gyw
DEFAULT_AI_PROVIDER=gemini
DEFAULT_AI_MODEL=gemini-2.0-flash

# ── Firecrawl (running on this same VM — use localhost) ───────────────────────
FIRECRAWL_API_KEY=
FIRECRAWL_BASE_URL=http://localhost:3002
FIRECRAWL_VM_IP=34.18.85.76

# ── GCP ───────────────────────────────────────────────────────────────────────
GCP_PROJECT_ID=uniapply-486919
GCP_ZONE=me-central1-a
FIRECRAWL_VM_NAME=firecrawl

# ── Pipeline Auth ─────────────────────────────────────────────────────────────
PORTAL_API_TOKEN=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiI2YjUzMzM1YS1mNjEwLTRhNjEtOThhMy1kOGM1MWM4YThjZmYiLCJleHAiOjE3NzMxMDY4MTF9.Zvjg2hYsyDAY0V9d6px4CPVQL6Hz9x51tNgZmINk_Zw

# ── Crawl4AI ─ NATIVE (no URL needed — runs in-process) ─────────────────────
# (step3_crawl.py uses AsyncWebCrawler directly, not the HTTP API)
